# CLAUDE.md — Saydog 작업 헌법

모든 세션이 자동으로 읽는 파일. **지시가 이 파일과 충돌하면 그냥 따르지 말고, 이 파일을 근거로 되묻는다.** 이 파일의 수정은 창업자가 명시적으로 지시했을 때만.

## 제품 본질 — 세 동작이 전부다

Saydog = 1인칭 약속 관리 (say-do gap 추적). MVP는 세 동작:

1. **지시하기** — `/capture` 단일 입구. 꼭지 분해는 **발송 전에** 일어나고, 분해 결과(요청 항목)는 메일 본문과 직답 페이지에 그대로 실린다. **일의 전후 원칙**: 상대가 본 구조에만 맞는 답이 온다 — 발송 후 구조 변경 금지.
2. **답변 받기** — 직답 버튼(무계정 공개 웹) 우선, 메일 회신은 폴백(안내문 포함). 상대방에게 가입·로그인을 요구하는 설계 금지.
3. **수행됨 처리** — 지시자가 내용을 보고 닫는다. **거울 원칙**: 시스템은 비추기만 하고, 완료는 사람이 확정한다. 자동 완료 금지.

이 밖의 기능은 기본 답이 "나중에(ROADMAP)"다.

## 절대 규칙 (되물음 없이 어기지 말 것)

- **서버는 사용자의 메일함을 읽지 않는다.** Gmail OAuth 스코프는 `gmail.send` 단독 — 읽기 스코프 추가 금지 (`tests/reply-token.test.ts`에 가드 테스트). 조회가 필요하면 브라우저-로컬 경로(`LocalGmail`)로.
- 숨긴 메뉴(전략 통일성·OKR/KPI·분석·조직도·프로세스 엔진)를 창업자 지시 없이 부활시키지 않는다.
- 지시 생성 입구는 `/capture` 하나. 두 번째 입구를 만들지 않는다.
- 실존 외부 이메일 주소로 테스트 발송 금지 — 테스트 계정만.
- push 전 필수: `npx tsx tests/run.ts` 전체 통과 + `npx tsc --noEmit` 클린. UI 변경은 `npx next build`까지. 스키마 변경은 마이그레이션 파일 필수 (start가 `migrate deploy`를 실행하므로 데이터 마이그레이션도 SQL로).
- 프로덕션에 영향 주는 베팅(예: "Gmail이 Message-ID를 보존한다")은 **베팅임을 명시하고 실패 시 증상·복구책을 함께 기록**한다.

## 작업 게이트 — 모든 기능 작업의 앞뒤

**시작 전, 한 줄로 답하고 시작한다:**
> "이 작업은 세 동작 중 [N]을 강화한다: (한 줄 근거)"

- 어느 동작도 강화하지 않으면 → 만들지 말고 되묻는다: *"이건 세 동작 밖입니다. ROADMAP으로 미룰까요, 그래도 지금 만들까요?"*
- 상대방이 보게 될 구조 ↔ 우리가 기대하는 답의 구조가 일치하는지 확인 (일의 전후).

**끝나기 전, Definition of Done:**
- [ ] 테스트·타입·(UI면) 빌드 통과 → push (Railway 자동 배포)
- [ ] 창업자용 검증 시나리오 1~3줄 제시 ("이렇게 눌러보면 이렇게 보입니다")
- [ ] 상태가 바뀌었으면 HANDOFF.md 갱신, 갈림길 결정은 DECISIONS.md에 한 줄
- [ ] 이전 발송분·기존 데이터에 소급 적용되는지/안 되는지 명시

## 문서 체계 (역할 고정 — 서로 침범 금지)

| 파일 | 역할 |
|---|---|
| CLAUDE.md | 헌법. 본질·절대 규칙·게이트 (창업자 지시로만 수정) |
| HANDOFF.md | 현재 상태·아키텍처·남은 일 (세션 인수인계) |
| DECISIONS.md | 내린 결정과 이유 (재론 방지) |
| ROADMAP.md | 미룬 것 (숨긴 기능·미래 카드) |

## 창업자 커뮤니케이션

- 답변은 한국어. 창업자가 한국어로 쓰면 끝에 영어 번역 연습 1줄, 영어로 쓰면 교정+조언 후 답변.
- 지시가 모호하면: 본질(세 동작)에 비추어 **가장 좁게** 해석한다. 기능이 늘어나는 방향의 해석은 반드시 확인 후 진행.
- 창업자의 "~안 되나?" 질문은 대개 아키텍처 탐색이다 — 평가·설명으로 답하고, "만들어/해줘/시작해" 신호가 있을 때 구현한다.
- 버그 보고("~개판 아니냐")에는: 재현 → 원인 확정 → 수정 배포 → 검증 레시피 순서로. 원인을 못 찾은 증상은 추측으로 덮지 말고 "확인 포인트"를 요청한다.
